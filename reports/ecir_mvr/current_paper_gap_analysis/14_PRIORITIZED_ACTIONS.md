# Prioritized actions

## TIER 0 — BLOCKER

1. Complete the preregistered matched multiseed run and its integrity audit; exclude invalid engineering runs only by frozen rules.
2. Freeze one formulation and exact step-17,500 checkpoint before any final/protected outcome read.
3. Evaluate the frozen formulation once on a genuinely untouched or authorized protected molecule-disjoint cohort. Do not tune afterward.
4. Create an immutable release commit/archive containing the exact executed runners, configs, manifests, checkpoint, evaluator, environment, and hashes.
5. Add a current-candidate matched conventional refinement comparison sufficient to answer “why not MMFF94s/GFN2-xTB optimization?” with same-record endpoints and runtime.

## TIER 1 — SHOULD FIX

1. Run the smallest final-formulation controls for Adaptive BA, predictive-sigma action weighting, and Bond-only/Angle-only action if those are claimed as necessary contributions.
2. Add sigma calibration/coverage diagnostics or explicitly remove calibrated-uncertainty language.
3. Report current-candidate throughput, latency, memory, and scaling under matched hardware/protocol.
4. Make Source RMSD definitions distinct in all tables and correct “explicit VJP”, Gauss–Newton, and optimization terminology.
5. Report molecule-level wins/losses and paired effect distributions, not only means/pass rates.

## TIER 2 — STRENGTHENING

1. Run current frozen formulation zero-shot on DiTMC/AvgFlow only if the paper seeks cross-upstream claims.
2. Expand subgroup/tail analysis across completed seeds for large, flexible, and poor-Source molecules.
3. Add a comparable published learned refiner if one can be run fairly without changing the task.
4. Provide qualitative local geometry examples linked to quantitative paired transitions.

## TIER 3 — OPTIONAL

- targeted beta or belief/post sensitivity after core evidence, without a broad ratio sweep;
- symmetry-aware Reference RMSD as a secondary analysis;
- individual tau-bound/cap/regularizer decomposition;
- extra seeds beyond the frozen three;
- set-level diversity/coverage metrics if no generation-set claim is made.

## Unnecessary extra work

- new controllers, trust regions, second-order/Gauss–Newton modules, or Teacher–Student Sigma revival;
- 30k/40k training extensions;
- broad hyperparameter or movement-cap sweeps;
- torsion architecture development merely to increase metric breadth;
- every-upstream adaptation when the claim is explicitly ETFlow-specific;
- repeated DEV optimization after multiseed.

```text
MODEL_DEVELOPMENT_STATUS = FREEZE_AFTER_MULTISEED
```

